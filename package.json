{
  "name": "tac-online",
  "version": "0.1.0",
  "private": true,
  "description": "Freies Online-Brett für das Brettspiel TAC (4 Spieler, 2 Teams)",
  "type": "module",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build",
    "build": "tsc --build",
    "dev:server": "npm run dev --workspace @tac/server",
    "dev:client": "npm run dev --workspace @tac/client"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.2.7"
  }
}
